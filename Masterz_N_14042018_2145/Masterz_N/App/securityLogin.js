angular.module('app.securityLogin', [])

    .controller('SecurityLoginController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });