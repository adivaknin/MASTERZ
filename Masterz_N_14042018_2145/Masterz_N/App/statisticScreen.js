angular.module('app.statisticScreen', [])

    .controller('StatisticScreenController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });