angular.module('app.systemPanel', [])

    .controller('SystemPanelController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });