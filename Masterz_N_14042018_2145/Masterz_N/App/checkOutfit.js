angular.module('app.checkOutfit', [])

    .controller('CheckOutfitController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });