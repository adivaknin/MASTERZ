angular.module('app.editExistEvent', [])

    .controller('EditExistEventController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });