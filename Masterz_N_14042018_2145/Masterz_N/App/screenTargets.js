angular.module('app.screenTargets', [])

    .controller('ScreenTargetsController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });