angular.module('app.luhotZmanim', [])

    .controller('LuhotZmanimController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });