angular.module('app.lessonSchedule', [])

    .controller('LessonScheduleController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });