angular.module('app.addNewLesson', [])

    .controller('AddNewLessonController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });