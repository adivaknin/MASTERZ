angular.module('app.addNewEvent', [])

    .controller('AddNewEventController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });