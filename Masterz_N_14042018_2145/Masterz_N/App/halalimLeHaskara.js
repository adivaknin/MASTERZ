angular.module('app.halalimLeHaskara', [])

    .controller('HalalimLeHaskaraController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });