angular.module('app.graficScreen', [])

    .controller('GraficScreenController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });