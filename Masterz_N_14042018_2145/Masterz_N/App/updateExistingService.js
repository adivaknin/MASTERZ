angular.module('app.updateExistingService', [])

    .controller('UpdateExistingServiceController', function ($scope, $http) {
        var isOved = true;
        var isManager = true;
        var isTrainer = true;
    });